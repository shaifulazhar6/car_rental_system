<!DOCTYPE html>
<html>
<head>
  <title>Car Rental System</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

  <div class="container mt-5">
    <div class="text-center">
      <h1 class="mb-4">Welcome to the Car Rental System</h1>
      <p class="lead">Book your ride, manage your rentals, and drive worry-free.</p>

      <!-- NEW instruction text -->
      <h5 class="mt-4 text-secondary">Login/Register as an Admin or User to continue</h5>

      <a href="/login" class="btn btn-primary me-2 mt-3">Login</a>
      <a href="/register" class="btn btn-outline-primary mt-3">Register</a>

      <!-- Show logged in user info -->
      <% if (user) { %>
        <p class="mt-4 text-success">
          You are logged in as <strong><%= user.username %></strong> (<%= user.role %>)
        </p>
      <% } %>
    </div>
  </div>

</body>
</html>

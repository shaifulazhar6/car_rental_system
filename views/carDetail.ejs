<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Car Details</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <%- include('navbar'); %>
  <div class="container mt-5">
    <h2>Car Details</h2>

    <table class="table table-bordered">
      <tr><th>Model</th><td><%= car.car_model %></td></tr>
      <tr><th>Type</th><td><%= car.car_type %></td></tr>
      <tr><th>Rate</th><td>$<%= Number(car.rental_rate).toFixed(2) %></td></tr>
      <tr><th>Term</th><td><%= car.rental_term %></td></tr>
      <tr><th>Available</th><td><%= car.availability === 1 ? 'Yes' : 'No' %></td></tr>
      <tr><th>From</th><td><%= car.available_from %></td></tr>
      <tr><th>To</th><td><%= car.available_to %></td></tr>
      <tr><th>Pickup</th><td><%= car.pickup_location %></td></tr>
      <tr><th>Image</th><td>
        <% if (car.image) { %>
          <img src="/images/<%= car.image %>" alt="Car Image" style="max-width: 300px;">
        <% } else { %>
          <em>No image uploaded</em>
        <% } %>
      </td></tr>
    </table>

    <a href="/cars" class="btn btn-secondary">Back to List</a>
  </div>
</body>
</html>
